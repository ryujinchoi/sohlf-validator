[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sohmns"
version = "0.1.0"
description = "SOHLF V3 & SO-HMNS Core Optimization Engine"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "torch>=2.0.0"
]
